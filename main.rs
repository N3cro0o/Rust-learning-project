fn main(){
    let mut a: i64 = 23; // let - varible declaration, I guess it's const without any modifications; mut - mutable
    let b: i64 = 123;
    a += b;
    println!("{a}"); // "{}" - to use variable as a string
}
